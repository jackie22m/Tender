info:
  name: Get Pets - Filtered (minHapiness)
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:9812/pets?minHappiness=40
  params:
    - name: minHappiness
      value: "40"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
