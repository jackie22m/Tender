info:
  name: Get Pets - Filtered (combined)
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:9812/pets?species=blob&minHappiness=40
  params:
    - name: species
      value: blob
      type: query
    - name: minHappiness
      value: "40"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
